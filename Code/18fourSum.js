/**
 * @param {number[]} num
 * @param {number} target
 * @return {number[][]}
 *
 */
var fourSum = function(nums, target) {
  var S = nums.sort(function(a, b) { return a > b ? 1 : -1; });
  var L = S.length;

  for (var i = 0; i < L - 3; i += 1) {
    
  }
};
